/**
 * SectionHeading
 * ─────────────────────────────────────────────────────────────────────────────
 * Backward-compatible wrapper — delegates to the canonical SectionHeader.
 * New code should import SectionHeader directly from "@/components/common".
 *
 * @deprecated Use SectionHeader from "@/components/common/SectionHeader"
 */

export { SectionHeader as SectionHeading } from "@/components/common/SectionHeader";
